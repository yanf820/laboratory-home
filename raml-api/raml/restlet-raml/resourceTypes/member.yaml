usage: |
  用于单个元素的操作
post?:
  body:
    application/json:
      schema: <<type>>
put?:
  body:
    application/json:
      schema: <<type>>
patch?:
  body:
    application/json:
      schema: <<type>>
get:
  responses:
    200:
      body:
        application/json:
          schema: <<type>>
type: baseResource
